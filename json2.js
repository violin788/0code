const fs = require('fs');
const utils = fs.readFileSync('util.js', 'utf-8');eval(utils);
function json2(inp) {
	
	readJSON('output.json', function(error, manifest){
	 
	    manifest.name
	    // => 'read-json'
	 
	})	
	
//comments before final bracket
}
inp = [];
json2(inp)